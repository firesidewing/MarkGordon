---
interface Props {
	variant?: 'default' | 'muted' | 'brand';
	narrow?: boolean;
	id?: string;
	class?: string;
}

const { variant = 'default', narrow = false, id, class: className } = Astro.props;
---

<section
	id={id}
	class:list={[
		'py-section-mobile sm:py-section-tight lg:py-section',
		variant === 'muted' && 'bg-surface-muted',
		variant === 'brand' && 'bg-brand text-text-inverse',
		className,
	]}
>
	<div
		class:list={[
			'mx-auto w-full px-4 sm:px-6 lg:px-8',
			narrow ? 'max-w-narrow' : 'max-w-content',
		]}
	>
		<slot />
	</div>
</section>
